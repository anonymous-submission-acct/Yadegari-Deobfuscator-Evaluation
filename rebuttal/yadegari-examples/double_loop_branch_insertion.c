#include <stdlib.h>

int main(int argc, char **argv)
{
  int nondeterm = atoi(argv[1]);
  int output = 0;

  for (int i = 0; i < nondeterm; i++)
  {
    if (nondeterm > 100)
    {
      output += 2;
    }
    else
    {
      output += 2;
    }
  }

  return output;
}
